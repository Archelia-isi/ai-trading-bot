{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn services.dashboard_engine.main:app --host 0.0.0.0 --port $PORT'"
  }
}
